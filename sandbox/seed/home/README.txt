Welcome to the Erdtree sandbox.

This is a throwaway environment — everything resets when you exit.
Poke around and test the system in plain English or bash.

Things to try:
  - "what files are in my home directory"
  - "find the largest files here"
  - "search my logs for errors"
  - "how many users are in the data file"
  - "show me the webapp config"
  - "clean up the downloads folder"      (tests the destructive-op gate)
  - "what python packages does the webapp need"

Directories:
  documents/   notes, a budget, a todo list
  projects/    a small webapp + some shell scripts
  downloads/   junk to clean up
  logs/        app + access logs (with some errors to find)
  data/        users.csv and products.json to search
