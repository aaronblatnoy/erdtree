app:
  name: webapp
  port: 8080
  workers: 4
database:
  host: db.internal
  port: 5432
  pool_size: 40
  name: appdb
cache:
  backend: redis
  ttl_seconds: 300
logging:
  level: info
  file: /var/log/webapp/app.log
