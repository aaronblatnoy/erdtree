# webapp

A small Flask service. Health check at /health, user count at /users.

## Run
    pip install -r requirements.txt
    python app.py

Config lives in config.yaml. Logs go to /var/log/webapp/app.log.
